import "./index.css";
import ReactDOM from "react-dom";
import React from "react";

const container = document.querySelector("body");

const elem = <h1 class="title">Todo List</h1>;

ReactDOM.render(elem, container);
